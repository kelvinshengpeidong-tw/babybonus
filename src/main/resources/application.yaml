spring:
  application:
    name: babybonus
  datasource:
    url: jdbc:h2:mem:babybonus;DB_CLOSE_DELAY=-1
    username: ${DB_USERNAME:admin}
    password: ${DB_PASSWORD:admin}
  h2:
    console:
      enabled: true
      path: /h2-console